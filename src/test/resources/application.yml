spring:
  shell:
    interactive:
      enabled: false
  h2:
    console:
      path: /h2-console
      settings:
        web-allow-others: true
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: update

    show-sql: true

